package com.mechchess.engine;

/**
 * Enum representing a piece side.
 * NOTE: enum was chosen over a class since chess sides have two constants: white and black.
 */
public enum Side {
    WHITE,
    BLACK
}
